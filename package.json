{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "artifacts/api-server",
    "lib/api-zod",
    "lib/db"
  ],
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "node artifacts/api-server/dist/index.mjs",
    "heroku-postbuild": "npm run build -w artifacts/api-server"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  }
}
