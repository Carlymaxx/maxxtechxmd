{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "node artifacts/api-server/dist/index.mjs",
    "heroku-postbuild": "npx pnpm@9 install && npx pnpm@9 --filter @workspace/api-server run build"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
